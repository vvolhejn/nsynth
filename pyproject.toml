[project]
name = "nsynth-tools"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "huggingface-hub>=1.5.0",
    "librosa>=0.10",
    "numpy",
    "soundfile",
    "tqdm",
]

[tool.uv]
dev-dependencies = []

